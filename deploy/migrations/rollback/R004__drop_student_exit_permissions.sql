-- !! EMERGENCIA APENAS. Apaga a tabela student_exit_permissions e TODOS os dados nela.
-- So use se a Fase D foi revertida no codigo E a tabela precisa sumir.
-- Na maioria dos casos NAO e' necessario: a tabela fica inerte e inofensiva.
-- ATENCAO: apaga as autorizacoes de saida cadastradas.
DROP TABLE IF EXISTS student_exit_permissions;
