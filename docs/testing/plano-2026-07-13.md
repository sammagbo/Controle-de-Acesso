# Plano de Testes — Segunda-feira 2026-07-13

**Commit alvo:** main @ 0f655b4 (registrar hash real do dia). **Ambiente:** PC-TRAB + terminal DS-K1T344MX (bench). **Executor:** Sam. **Formato de cada teste:** os campos fixos estão no cabeçalho de cada bloco (ambiente/responsável/pré-condições herdados do Bloco A); cada linha traz ID · título · objetivo · prioridade(P1..P3) · categoria · passos/comando · esperado (terminal/backend-log/HTTP/SQL/frontend) · evidência · limpeza. Status a preencher em `registro-resultados.md`: PASSOU/FALHOU/BLOQUEADO/NÃO APLICÁVEL.

**Categorias:** [AGORA]=executável hoje com 1 terminal · [SIM]=simulável (DoorMapping temporário, UI manual ou curl) · [4T]=exige os 4 terminais da cantina · [DEP-SI]/[DEP-FAB]/[DEP-VLAN]=dependências externas.

**Gates (ordem obrigatória):** A→B→C→D→E. Não iniciar bloco sem 100% dos P1 do anterior PASSOU. Nunca limpar dados antes de salvar evidência. Baseline SEMPRE antes de qualquer mudança de código (F5c/F5d proibidos hoje). Webhook definitivo já validado (T6/T7 concluídos em 09-10/07) — captura só se payload divergir.

## Bloco A — Preparação (todos P1, [AGORA])
PREP-01 Git: `git pull` → `git status` limpo, branch main, anotar hash. Evid: print.
PREP-02 Containers: `docker ps -a` → parar `magbo-db`/`meetingmanager_db` se Up; `docker start magbo-postgres` → Up. Evid: print.
PREP-03 Backup pré-teste (skill backup-restauracao) → arquivo em backups/. Evid: nome+tamanho.
PREP-04 Backend: 4 env vars + mvn run → `Started MagboAccessApplication` + 2 WARNs esperados. Evid: trecho log.
PREP-05 Health: CONNECTED. Evid: JSON.
PREP-06 IP do PC: `ipconfig` → anotar; conferir/ajustar Écoute HTTP no terminal. Evid: print dos dois.
PREP-07 IP/ping do terminal: display + `ping -n 2` 0% perda. Evid: print.
PREP-08 DoorMapping: GET /api/admin/door-mappings → id15 ativo com terminal_ip = IP atual (PUT se mudou). Evid: JSON.
PREP-09 Usuário 9999999 ativo (SELECT). 8888888 NÃO deve existir ainda. Evid: SQL.
PREP-10 Limpeza controlada: `DELETE FROM access_logs WHERE user_id IN ('9999999','8888888');` (logs antigos de bench) — SÓ após conferir que evidências de 09-10/07 já estão salvas. Evid: contagem antes/depois.
PREP-11 Pasta de evidências do dia criada (estrutura abaixo) + desativar suspensão do Windows. 
**GATE A:** tudo PASSOU → Bloco B.

## Bloco B — Cantina
Pré: mapping id15=REFEI1/ENTRADA. Janela SQL de conferência: `SELECT id,user_id,point_id,action,flag,timestamp FROM access_logs ORDER BY id DESC LIMIT 5;`
CANT-01 [AGORA][P1] Entrada válida: rosto → log `REFEI1/ENTRADA/flag=null/fallback=false`; UI/monitor reflete ≤5s. Evid: log+print+SQL.
CANT-02 [AGORA][P1] Entrada repetida (baseline dedup): 2º rosto ~30s depois → **2 registros** (sem dedup ainda — comportamento esperado; base do F5c). Evid: SQL 2 linhas.
CANT-03 [AGORA][P1] Refeição duplicada mesma janela: 3º rosto ~2min → 3º registro; `refectory/meals` pareia só 1ª ENTRADA. Evid: JSON meals.
CANT-04 [AGORA][P1] Fora de horário (antes/depois): INSERT class_schedules TESTE tudo 'N' → rosto → `flag=FORA_HORARIO` e registro salvo (observação, não bloqueio); KPI blockedToday incrementa. Limpeza: DELETE da linha TESTE. Evid: log+SQL+print KPI. *Variante "antes do horário" real: seg é MONDAY → alternativa: lun_midi='23:00' → rosto de manhã = antecipado.*
CANT-05 [AGORA][P1] Usuário desconhecido (F5b): pessoa 8888888 no terminal SÓ com senha → autenticar por senha → warn `sem correspondencia`, **nenhum** AccessLog; anotar subEventType do evento de senha (F5d). Evid: log.
CANT-06 [AGORA][P2] Usuário inativo: `UPDATE app_users SET ativo=false WHERE id='9999999';` → rosto → comportamento atual (registrar o observado; hoje o webhook não checa `ativo` — provável log normal = achado p/ decisão). Reverter UPDATE. Evid: log+SQL.
CANT-07 [AGORA][P1] Autenticação por rosto — coberto por CANT-01 (marcar PASSOU junto).
CANT-08 [AGORA][P2] Autenticação por senha (usuário válido): senha p/ 9999999 no aparelho → autentica → evento chega? subEventType? vira log? Evid: log.
CANT-09 [AGORA][P1] Evento negado (F5d): validade de 9999999 no passado → rosto → voz nega → **anotar se chega evento e qual subtipo** (não chegar também é resposta). Restaurar validade. Evid: log/ausência.
CANT-10 [SIM][P2] Entrada terminal Alto vs Baixo: PUT alternando label/mapping simula os dois pontos físicos; [4T] valida de verdade.
CANT-11 [SIM][P1] SAÍDA cantina: PUT id15 action=SAIDA → rosto → `REFEI1/SAIDA`; se ENTRADA anterior >1h → `EXCEDEU_TEMPO` (testar as duas variantes: logo após entrada = flag null; com entrada antiga = flag). Voltar mapping p/ ENTRADA ao fim. Evid: logs.
CANT-12 [4T] Dois terminais mesma pessoa / quase-simultâneo / ENTRADA-SAÍDA-ENTRADA na janela de dedup → BLOQUEADO até hardware; baseline lógico registrado em CANT-02/03/11.
CANT-13 [AGORA][P2] Professor/funcionário: `UPDATE app_users SET hikvision_employee_id=NULL WHERE id='P001'`? NÃO — usar seed real: criar user tipo PROFESSOR id 7777777 (INSERT à la T3) + rosto no aparelho OU senha; verificar log e Rapport (professores não contam em uniqueStudents? conferir queries). Limpeza no fim. Evid: SQL+JSON overview.
CANT-14 [AGORA][P2] Pessoa cadastrada no MAGBO mas não no aparelho: id existente sem rosto no terminal → nada acontece no aparelho (esperado; anotar como comportamento). 
CANT-15 [AGORA][P1] Dashboard/KPIs/polling/banco: após CANT-01..05, conferir GlobalStats (totalToday=Σ, blockedToday=flags), CantineMonitor 3s, Rapport Général (áreas/por hora), SQL de fechamento. Evid: prints+JSON+SQL.
**GATE B:** P1 todos PASSOU (CANT-12 pode ficar BLOQUEADO) → Bloco C.

## Bloco C — Biblioteca (CDI) — sem hardware dedicado: 2 vias
Via 1 [SIM-UI]: BibliotecaView (scan manual por ID). Via 2 [SIM-HW]: PUT id15 → pointId=BIBLIO/ENTRADA temporário (restaurar ao fim do bloco).
BIBL-01 [P1] Entrada: scan/rosto → log BIBLIO/ENTRADA; aluno aparece "presente".
BIBL-02 [P1] Saída/encerramento: novo scan (toggle) ou POST manual SAIDA → presença encerra; duração calculada.
BIBL-03 [P1] Tempo total/timer/alerta: ActiveTimers conta; permanência longa destacada. Evid: print com tempo.
BIBL-04 [P2] Permanência duplicada: 2ª ENTRADA sem SAIDA → presença mantém (último log manda); registrar comportamento.
BIBL-05 [P2] Saída sem entrada: POST SAIDA isolado → relatórios ignoram (pareamento exige ENTRADA primeiro). Evid: JSON meals-like/ausência.
BIBL-06 [P1] Persistência após reinício: fechar/reabrir Electron (e reiniciar backend) → presença recalculada dos logs (não de localStorage). Evid: prints antes/depois.
BIBL-07 [P2] Perda de conexão: parar backend 1min com painel aberto → ConnectionStatus acusa; religar → recupera sem duplicar. Evid: prints.
**GATE C** → Bloco D.

## Bloco D — Portão principal (preparação; execução real depende de infra)
PORT-01 [SIM][P1] Payload DeepinView simulado: curl JSON `EventNotificationAlert` (skill teste-acesso) com ipAddress .167 → mapping seed .167→PORT1/ENTRADA resolve; log PORT1. Prova o ramo JSON do parsePayload.
PORT-02 [SIM][P1] Direção: repetir com .166 → PORT1/SAIDA.
PORT-03 [SIM][P2] Aluno×desconhecido×negado no formato câmera (variar employeeNoString/subEventType) → F5b/whitelist futura.
PORT-04 [DEP-VLAN/SI][BLOQ] Captura de payload REAL das câmeras: exige backend alcançável na 192.168.1.x (VM Plano A, regra firewall Plano B, OpenAPI HCP Plano C). 
PORT-05 [DEP-FAB][P2] Confirmar no HCP: grupo/nível de acesso aplicado; investigar o controlador extra (1 porta anomalia, 34 pendentes); diferença operacional câmera×terminal documentada.
PORT-06 [4T/futuro] Painel aluno/responsável no portão — [PEND] de produto, fora do dia 13.
**GATE D** (só simuláveis) → Bloco E.

## Bloco E — Falhas e recuperação
FALH-01 [P1] Backend parado: derrubar 2min → rostos → religar → **fila do aparelho reentrega** (já observado; formalizar com evidência). 
FALH-02 [P1] Banco parado: `docker stop magbo-postgres` → rosto → log de erro 500 no backend (evento se perde? anotar!); religar; conferir estado. *Achado esperado: sem retry app-side — registrar p/ decisão.*
FALH-03 [P2] Frontend parado: sem impacto no pipeline (só visual). Confirmar.
FALH-04 [P1] Terminal sem rede: tirar cabo → rosto → religar → reentrega. 
FALH-05 [P1] Token inválido/ausente: curl com token errado → 401; sem token → 401/503 conforme config. Evid: HTTP codes.
FALH-06 [P1] Mapping inexistente: `PUT id15 ativo=false` → rosto → `fallback=true PORT1` (registro não se perde). Reativar.
FALH-07 [P1] Payload inválido: curl corpo lixo → 200 + warn `nao parseavel` (sem retry storm). 
FALH-08 [P2] Evento duplicado exato (reenvio): repetir curl idêntico → hoje gera 2 logs (sem dedup por serialNo — baseline F5c+).
FALH-09 [P1] IP alterado: procedimento do troubleshooting executado em <5min (simular trocando Écoute HTTP p/ IP errado e corrigindo).
FALH-10 [P2] Timezone: conferir/corrigir fuso do terminal p/ GMT-3 + hora → payload dateTime coerente (sem impacto no banco; evidenciar).
FALH-11 [P1] Reinício backend/banco em sequência + rosto → pipeline volta sozinho (containers restart=unless-stopped na VM; no PC é manual — anotar diferença).
**GATE E:** encerramento — backup pós-teste, T9-limpeza (DELETE users/logs de teste 9999999/8888888/7777777 + schedules TESTE + mappings extras), consolidar `registro-resultados.md`, arquivar evidências.

## Evidências — estrutura e conteúdo mínimo
`docs/testing/evidencias/2026-07-13/{01-preparacao,02-cantina,03-biblioteca,04-portaria,05-falhas,logs,payloads,sql,screenshots}`
Por teste: print da UI OU trecho de log (copiado como texto) + comando executado + resposta HTTP quando houver + SELECT de conferência. Cabeçalho do dia (uma vez): hash do commit, modelo+firmware do terminal, IP do PC, IP do terminal, hora de início. Vídeos: só p/ CANT-01 e BIBL-01 (opcional). Payloads brutos → `payloads/`; SQL → `sql/`; logs longos → `logs/`.
